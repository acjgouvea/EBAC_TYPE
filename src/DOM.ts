const campoNome = document.getElementById('nome');
const formulario = document.getElementsByTagName('form')
const botaoEviar = document.getElementById('bnt-enviar')

botaoEviar?.addEventListener('click', (e: MouseEvent) => {
})